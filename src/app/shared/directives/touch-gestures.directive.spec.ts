import { TouchGesturesDirective } from './touch-gestures.directive';

describe('TouchGesturesDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchGesturesDirective();
    expect(directive).toBeTruthy();
  });
});
